var assert = chai.assert

describe("main", function () {

    var utils = lymph.require("lymph-core").utils

    it("has a sanity check", function () {
        assert.equal(true, true)
    })

    describe("disk usage view", function () {

        it("has an identifing label", function () {
        })

        it("has an icon displaying the disk's type", function () {
        })

        it("shows the total capacity", function () {
        })

        it("shows the current usage", function () {
        })

        it("shows the amount of free space", function () {
        })

        it("shows a progress bar of the precentage being used", function () {
        })
    })
})

